// Blog page (Phase 1).
//
// On the live WordPress site this page is replaced by WordPress's native
// blog (Gutenberg posts on `tqps_post` archive + single templates in the
// theme: index.php / single.php). The empty-state copy shown in
// blog.html is what visitors see until the first WP post is published.
//
// No JS needed during static preview. Kept as an empty file so existing
// <script> references don't 404 if the HTML hasn't been re-built.
